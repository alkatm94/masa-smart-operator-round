# MASA industrial model slot

Run `training/export_model.py` to create `model.onnx` and `model.json`. Until both exist, the UI reports `MASA Industrial Model: Not Trained`; it never invents an industrial label. See `training/README.md` and `docs/industrial-dataset-guide.md`.
