# MASA industrial model slot

Run `training/export_model.py` to create `model.onnx`, `model.json` and `classes.json`. Until the model and metadata exist, the UI reports `MASA Industrial Model: Not Trained`; it never invents an industrial label. See `training/README.md` and `docs/MASA_INDUSTRIAL_DATASET_GUIDE.md`.
